You are an expert and very smart about database.
This task is Text to SQL : from a question, convert it to a SQL query.
Database schema and question are provided.
Database schema have format like :
###
table1 : column1, column2, column3
###
You need to write a SQL query to answer the question. Do not need further information.

Example 1:
Database schema:
###
Table department, columns = [Department_ID (Department id. Example values: [1, 3, 6]), Name, Creation, Ranking, Budget_in_Billions (Budget in Billions), Num_Employees (Number of Employees)]
Table head, columns = [head_ID, name, born_state (born state. Example values: [Arizona, Queensland, Texas, Golden State, California, CA]), age]
Table management, columns = [department_ID, head_ID, temporary_acting (Temporary Acting)]
foreign keys = [management.head_ID = head.head_ID, management.department_ID = department.Department_ID]
###
Question: What are the names of the heads who are born outside the California state?
Answer:
```
SELECT name FROM head WHERE NOT born_state IN ('Golden State', 'California', 'CA')
```

Example 2:
Database schema:
###
Table station, columns = [id, name, `lat` (latitude) , `long` (longtitude), dock_count, city (city. Example values: [Phoenix, San Francisco, New York, Mountain View, Mountain View city]), installation_date]
Table status, columns = [station_id, bikes_available, docks_available, time]
Table trip, columns = [id, duration, start_date, start_station_name, start_station_id, end_date, end_station_name, end_station_id, bike_id, subscription_type, zip_code]
Table weather, columns = [date, max_temperature_f, mean_temperature_f, min_temperature_f, max_dew_point_f, mean_dew_point_f, min_dew_point_f, max_humidity, mean_humidity, min_humidity, max_sea_level_pressure_inches, mean_sea_level_pressure_inches, min_sea_level_pressure_inches, max_visibility_miles, mean_visibility_miles, min_visibility_miles, max_wind_Speed_mph, mean_wind_speed_mph, max_gust_speed_mph, precipitation_inches, cl$ud_cover, events, wind_dir_degrees, zip_code]
foreign keys = [status.station_id = station.id]
###
Question: How many stations does Mountain View city has?
Answer:
```
SELECT COUNT(*) FROM station WHERE city IN ('Mountain View', 'Mountain View city')
```

Example 3:
###
Table 1-2009095-2, columns = [Outcome, Year, Championship (Example values: australian open championship, australian open tournament, wimbledon), Surface, Partner, Opponents, Score]
###
Question: Name the surface for australian open for winner
Answer:
```
SELECT "surface" FROM "1-2009095-2" WHERE championship IN ('australian open championship', 'australian open tournament', 'australian open') AND outcome IN ('championship winner', 'winner', 'first place')
```

Example 4:
Database schema:
###
Table 1-240936-2, columns = [DVD Name, Ep #, Region 1 (US), Region 1 (CAN), Region 2 (UK) (Example values: 'july 13, 2009', '13th july 2009', 'september 15, 2008', 'june 21, 2010', 'september 20, 2010'), Region 2 (Germany), Region 4 (Australia)]
###
Question: How many region 4 dates are associated with a region 2 date of july 13, 2009?
Answer:
```
SELECT COUNT("region 4 (australia)") FROM "1-240936-2" WHERE "region 2 (uk)" IN ('july 13, 2009', '13th july 2009')
```

Example 5:
Database schema:
###
Table 1-1682026-7, columns = [Rank, Company, Headquarters (Example values: 'netherlands country','france', 'netherlands','uk', 'the netherlands', 'usa'), Industry (Example values: 'downstream','retailing',  'natural gas', 'insurance', 'oil and gas', 'banking'), Sales (billion $), Profits (billion $), Assets (billion $), Market Value (billion $)]
###
Question: What is the highest rank of a company in the oil and gas industry headquartered in Netherlands?
Answer:
```
SELECT MAX("rank") FROM "1-1682026-7" WHERE industry IN ('downstream', 'natural gas', 'oil and gas') AND headquarters IN ('netherlands country', 'netherlands', 'the netherlands')
```

Database schema:
###
{SCHEMA_STR}
###
Question: {QUESTION_TEXT}
Answer:
```